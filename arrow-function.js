const dividing = number => number / 5;
const result = dividing(50);
// console.log(result);
const multiply = (num1, num2) => {
    const number1 = num1 + 2;
    const number2 = num2 + 2;
    const result = number1 * number2 ;
    return result;
}
const total = multiply(40, 10);
// console.log(total);

const multiplyResult = (x , y, z) => x * y * z;
const gunpol = multiplyResult(10, 20, 30);
console.log(gunpol);


